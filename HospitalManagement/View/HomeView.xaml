<UserControl x:Class="HospitalManagement.View.HomeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:HospitalManagement.View"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1100">

    <Border Background="White" CornerRadius="0,8,8,0" SnapsToDevicePixels="True">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Button Content="Páciensek" Style="{StaticResource HomeBtnStyle}" Margin="150,0,0,0"/>
            </Grid>

            <Grid Grid.Column="1">
                <Button Content="Alkalmazottak" Style="{StaticResource HomeBtnStyle}" Margin="0,0,150,0"/>
            </Grid>
        </Grid>
    </Border>
    
</UserControl>
